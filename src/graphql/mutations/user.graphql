mutation CreateUser($input: CreateUserInput!) {
  createUser(input: $input) {
    email
    name
    firebaseUId
  }
}

mutation SignIn($input: SignInInput!) {
  signIn(input: $input) {
    email
    name
    firebaseUId
  }
}
